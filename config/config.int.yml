database:
    type: postgres
    database: infect
    schema: rda_lock_service
    hosts:
        - host: ${SERVICE_REGISTRY_DB_HOST}
          username: postgres
          password: ${LOCK_SERVICE_DB_PASSWORD}
          port: 5432
          pools:
            - read
            - write
          maxConnections: 20